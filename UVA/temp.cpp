#include <iostream>
#include <vector>
#include <string>
#include <math.h>

void permutations(const int& n){
  std::vector<std::string> v(n);//v(static_cast<int>(pow(2,n)));
}

int main(){
  int n;
  while(n != 0){
    permutations(n);
  }
    
}
